// API configuration
// In production, this will be set by Vercel environment variable VITE_API_URL
// In development, it uses the Vite proxy (see vite.config.js)
export const API_BASE_URL = import.meta.env.VITE_API_URL || '/api'
